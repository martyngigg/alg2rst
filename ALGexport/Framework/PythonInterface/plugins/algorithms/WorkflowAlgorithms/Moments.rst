.. algorithm:: Moments

.. summary:: Moments

.. aliases:: Moments

.. usage:: Moments

.. properties:: Moments

Calculates the :math:`n^{th}` moment :math:`M_n` of :math:`S(Q,w)` where
:math:`M_n` is the integral of :math:`w^n*S(Q,w)` over all w for
:math:`n=0` to 4.

.. categories:: Moments